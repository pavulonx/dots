# chezmoi config
data:
  conf:
    profile:      '%%!profile!%%'
    browser:      '%%!browser!%%'
    pid1:         '%%!pid1!%%'
    user:
      email:      '%%!u_email!%%'
      fullname:   '%%!u_fullname!%%'

merge:
  command: "nvim"
  args: "-d"

pager: delta

# vi: syntax=yaml
